---
# - hosts: all
#   gather_facts: true
#   become:
#     true
#   become_user:
#     root
#   remote_user:
#     centos
#   tasks:
#    - name: install nano
#      yum: name=nano state=latest
- hosts: 
      all
  become:
    true
  become_user:
    root
  remote_user:
    centos
  roles:
     - node-exporter

- hosts: 
    proxy
  become:
    true
  become_user:
    root
  remote_user:
    centos
  roles:
    # 
    # - letsencrypt
     - nginx-proxy
    #  - letsnginx
     
     
- hosts: 
      app
  become:
    true
  become_user:
    root
  remote_user:
    centos
  roles:
     - nginx


  
- hosts: 
    monitoring
  become:
   true
  become_user:
   root
  remote_user:
    centos
  roles:
   - prometheus

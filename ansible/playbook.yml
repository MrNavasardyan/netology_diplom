---
- hosts: 
      proxy
  become:
    true
  become_user:
    root
  remote_user:
    centos
  roles:
     - nginx

# - hosts: nodes
#   become:
#     true
#   become_user:
#     root
#   remote_user:
#     centos
#   roles:
#    - nodeexport

